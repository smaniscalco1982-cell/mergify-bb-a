pull_request_rules:
  - name: dismiss approvals when new commits are pushed
    conditions: [base=main]
    actions: { dismiss_reviews: { approved: true } }
  - name: merge once approved and labelled
    conditions: ["#approved-reviews-by>=1", label=ready]
    actions: { merge: { method: merge } }
